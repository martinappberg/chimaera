---
description: Hard constraints for the Tauri native shell — its standalone workspace, the command quad-lockstep, and version stamping.
paths: ["crates/chimaera-app/**"]
---

# Native-shell rules

Depth: [chimaera-app/AGENTS.md](../../crates/chimaera-app/AGENTS.md).

- **This is a standalone cargo workspace.** Tauri stays out of the daemon workspace so
  musl/HPC builds stay lean; the daemon crates compile twice (intentional). Never fold
  the app into the root workspace. Root `cargo` doesn't build it — use `just app-*`.
- **The command surface is a lockstep** the compiler does not fully enforce: `shell.rs`
  `generate_handler!` ↔ `command_manifest.rs` (shared by `build.rs` + runtime grants) ↔ committed
  `permissions/autogenerated/*.toml` (plus `native.ts` wrappers). Change a command in
  all of them, then `npx tauri build` to confirm they agree. Exception: `wsl_*`
  commands are wizard-only — granted by `capabilities/wsl-setup.json` (label-scoped,
  local-only, no `native.ts` wrapper), never by the runtime daemon-window capability, because daemon-served
  pages include remote hosts' UIs and `wsl_install` pops UAC.
- **Version stamping matches the literal `0.0.1` sentinel** via `sed` across
  `tauri.conf.json` + both `Cargo.toml`s. A pre-bumped value makes the sed silently
  no-op and ship the wrong version.
- **`--askpass` must stay lightweight** — it's the `SSH_ASKPASS` relay and must never
  spawn a daemon or window. The four-role argv dispatch order in `main.rs` is
  load-bearing; the `board` arm (the shim's `current_exe()` target here is this GUI
  binary) must exit before any Tauri init, or the single-instance plugin fronts the
  running window and swallows the CLI.
- Signing is release-only (`TAURI_SIGNING_PRIVATE_KEY*`); the `app.yml` PR build needs
  no key.
