/target
node_modules/
web-ui/dist/
# The native shell is a standalone cargo workspace (Tauri stays out of the
# daemon workspace), so it has its own target dir.
crates/chimaera-app/target/
crates/chimaera-app/gen/
*.log
.DS_Store
.claude/scheduled_tasks.lock
.claude/worktrees/
# Isolated per-worktree state: the bare daemon (chimaerad-isolated /
# serve-isolated.sh) and the full app (app-dev-isolated / run-app-isolated.sh).
.chimaera-dev/
.chimaera-dev-app/
verify-tmp/
.claude/settings.local.json
